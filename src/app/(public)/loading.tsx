import { AuthPremiumLoader } from '@/components/ui/auth-premium-loader';

export default function PublicLoading() {
  return <AuthPremiumLoader title="Preparando tu espacio…" description="Estamos cargando Flowtask." />;
}
