# FlowTask — V58.15.4 Media System Final

Base: `v58.15.3.9 Project List and Title Input Fix`.

## Cambios
- Activa lectura real de `projects.image_url` en listado y detalle de proyectos.
- Mantiene upload de imagen de proyecto desde el formulario y permite limpiar la imagen guardando `null`.
- Refuerza avatar/foto de cliente con límite de 5 MB y persistencia en `clients.avatar_url`.
- Mantiene separación de workspace individual vs organización y no cambia Supabase salvo la migración ya aplicada de v58.15.3.5.
- Actualiza metadata y scripts de release a `58.15.4-media-system-final`.

## Migración requerida
Debe estar aplicada:

```sql
alter table public.projects add column if not exists image_url text;
alter table public.clients add column if not exists avatar_url text;
```

## Validaciones
- `npm run verify:v58.15.4`
- `npm run deploy:readiness`
- `npm run deploy:production:ready`

`typecheck/build` requieren `node_modules` y variables `.env.local` en ambiente local/Vercel.
