# Audit — v58.28.21.2 Classic Project Edit Form Alignment

Alcance:

- `ProjectHeroInlineEditor` usa un shell dedicado `ft-project-edit-shell`.
- La edición queda organizada en cover + copy + meta grid + progreso.
- Los controles de imagen viven sobre el cover.
- No se altera la organización inferior de tabs, miembros, archivos, tareas internas ni actividad.
