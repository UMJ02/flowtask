# FlowTask V58.14.3 — Tareas Vistas 2026

Base: V58.14.2.1 Registros Premium Refine FULL.

## Archivos modificados
- `src/components/tasks/task-action-list.tsx`

## Qué cambia
- Nueva estructura de vistas: `Lista | Tablero | Calendario | Timeline ▼`.
- Dentro de Timeline: `Smart Timeline`, `Gantt Builder`, `Guardar vista`, `Exportar`.
- Calendar pasa a una vista mensual premium tipo Operations Calendar Pro.
- Timeline agrega una vista híbrida Smart Timeline conectada a tareas reales.
- Gantt agrega un Custom Gantt Builder con panel de personalización, exportación y guardado local.
- Lista y Tablero mantienen CRUD y lógica existente.
- Acciones de ver, editar, finalizar, borrar y selección masiva se conservan.

## Reglas respetadas
- No se tocó Supabase.
- No se tocaron migrations.
- No se tocaron queries.
- No se tocaron package.json ni vercel.json.
- No se cambió el backend.

## Validación realizada en este entorno
- Parse TSX del archivo modificado: OK.
- `node scripts/verify-v58.13.6.mjs`: OK.
- `node scripts/build-deploy-readiness.mjs`: OK.
- `node scripts/client-readiness-check.mjs`: OK.
- `npm ci` no terminó por timeout del entorno, por eso no se afirma build completo local aquí.

## Comandos recomendados para validar local
```bash
rm -rf node_modules .next
npm ci
npm run typecheck
npm run build
```
